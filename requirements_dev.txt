# This file lists the dependencies for development and testing of the project.

# Project dependencies
py-youtube==1.1.7
ensure==1.0.2
mkdocs-material

## Development dependencies
pytest>=7.4.0
tox>=4.11.0
tox-gh-actions>=3.1.0
flake8>=6.1.0
mypy>=1.5.0

-e .
## some time its create issue in (-e .) so we can remove it and install the package using pip install -e . command